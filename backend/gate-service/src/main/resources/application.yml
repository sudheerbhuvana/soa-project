server:
  port: 8085

spring:
  application:
    name: gate-service
  datasource:
    url: jdbc:postgresql://ep-flat-breeze-b5j4ye6o-pooler.c-7.us-east-2.aws.neon.tech:5432/soa-postgres?sslmode=require&channel_binding=require
    username: soa-postgres_owner
    password: npg_Rc6hqlZ3Dawv
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 10000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: gate
        format_sql: false
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
